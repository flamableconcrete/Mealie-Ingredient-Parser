# Mealie Ingredient Parser Configuration
# Copy this file to .env and fill in your values

# ==============================================================================
# Required Settings
# ==============================================================================

# Your Mealie API key (required)
# Get this from your Mealie instance: Settings -> API Tokens
MEALIE_API_KEY=your_api_key_here

# Your Mealie instance URL (required)
# Should end with /api (e.g., https://mealie.yourdomain.com/api)
MEALIE_URL=https://your-mealie-instance.com/api

# ==============================================================================
# Performance Settings
# ==============================================================================

# Number of items to process in parallel during batch operations (default: 10)
# Higher values = faster processing but more API load
# Lower values = slower but safer for rate-limited APIs
#BATCH_SIZE=10

# ==============================================================================
# UI Customization - Column Widths
# ==============================================================================
# Adjust the width of columns in batch mode tables
# Useful if your ingredient names, units, or foods are particularly long/short
# All values are in characters

# Width of the "Pattern Text" column (default: 50)
# Shows the unparsed ingredient pattern text
#COLUMN_WIDTH_PATTERN_TEXT=50

# Width of the "Status" column (default: 15)
# Shows parsing/matching status (matched, unmatched, etc.)
#COLUMN_WIDTH_STATUS=15

# Width of the "Parsed Unit" column (default: 40)
# Shows the unit extracted by the parser
# Increase this if your unit names are being truncated
#COLUMN_WIDTH_PARSED_UNIT=40

# Width of the "Parsed Food" column (default: 40)
# Shows the food/ingredient extracted by the parser
# Increase this if your food names are being truncated
#COLUMN_WIDTH_PARSED_FOOD=40

# Width of the "Create" checkbox column (default: 8)
# Usually doesn't need adjustment unless using a different font
#COLUMN_WIDTH_CREATE=8
